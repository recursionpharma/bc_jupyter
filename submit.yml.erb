---
script:
  native:
  - "--mem=<%= memory.blank? ? 2 : memory.to_i %>G"
  - "--cpus-per-task=<%= num_cores.blank? ? 1 : num_cores.to_i %>"
  - "--nodelist=<%= node_select.blank? ? 'mem1' : node_select %>"
batch_connect:
  template: "basic"
